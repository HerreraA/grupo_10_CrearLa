<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>

    <body>
        <%- include('../partials/header') %>
            <main>
                <h1 class="servicios-h1">Categorías de servicios</h1>
                <div class="servicios-p">
                    <p>Omnis voluptate alias magni iste asperiores et nulla, modi molestiae aliquid perspiciatis eum vel
                        similique doloribus, provident totam.</p>
                </div>
                <% categorias.forEach(categoria=> { %>
                    <div class="box-servicio">
                        <div class="box-servicio-left">
                            <div class="box-servicio-number">
                                <%= categoria.id %>
                            </div>
                            <div class="box-servicio-txt">
                                <h2>
                                    <%= categoria.nombre %>
                                </h2>
                                <p>
                                    <%= categoria.descripcion %>
                                </p>
                            </div>
                            <div>
                                <button class="box-servicio-button">
                                    <a href="/categories/detail/<%= categoria.id %>">VER DETALLES</a>
                                </button>
                            </div>
                        </div>
                        <div class="box-servicio-right">
                            <img src="<%= categoria.imagen %>" alt="<%= categoria.nombre %>">
                        </div>
                    </div>
                    <% }) %>
            </main>
            <%- include('../partials/footer') %>
    </body>

</html>