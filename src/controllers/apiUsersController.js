const db = require ("../database/models")
module.exports= [
    all: async (req, res) => {
        let response = {
            count: 0,
            countByCategory: {},
            products: [], 
        }
        
    }

]